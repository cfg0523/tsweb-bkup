<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.techsen.tsweb.sys.dao.AuthDao">
    <resultMap type="Auth" id="AuthMap">
        <id property="id" column="auth_id"/>
        <result property="authName" column="auth_name"/>
        <result property="authDesc" column="auth_desc"/>
        <result property="createBy" column="auth_create_by"/>
        <result property="createDate" column="auth_create_date"/>
        <result property="updateBy" column="auth_update_by"/>
        <result property="updateDate" column="auth_update_date"/>
        <result property="remark" column="auth_remark"/>
    </resultMap>

    <sql id="UserTable">sys_user_mstr</sql>
    <sql id="UserRoleTable">sys_userrole_det</sql>
    <sql id="RoleTable">sys_role_mstr</sql>
    <sql id="RoleAuthTable">sys_roleauth_det</sql>
    <sql id="AuthTable">sys_auth_mstr</sql>

    <sql id="AuthColumns">
        auth_id, auth_name, auth_desc, auth_create_by,
        auth_create_date, auth_update_by,
        auth_update_date,
        auth_remark
    </sql>

    <sql id="AuthProperties">
        #{id}, #{authName}, #{authDesc}, #{createBy},
        #{createDate}, #{updateBy},
        #{updateDate},
        #{remark}
    </sql>

    <sql id="AuthUpdateMap">
        auth_name = #{authName},
        auth_desc = #{authDesc},
        <!-- auth_create_by = #{createBy}, -->
        <!-- auth_create_date = #{createDate}, -->
        auth_update_by = #{updateBy},
        auth_update_by = #{updateDate},
        auth_remark = #{remark}
    </sql>

    <sql id="AuthWhereCondition">
        <where>
            <choose>
                <when test="id != null">
                    auth_id = #{id}
                </when>
                <when test="authName != null">
                    auth_name = #{authName}
                </when>
                <otherwise>
                    1 == 0
                </otherwise>
            </choose>
        </where>
    </sql>

    <insert id="addEntity" parameterType="Auth">
        insert into <include refid="AuthTable"/> (
            <include refid="AuthColumns"/>
        ) values (
            <include refid="AuthProperties"/>
        )
    </insert>

    <delete id="deleteEntity" parameterType="User">
        delete from <include refid="AuthTable"/>
        where auth_id = #{id}
    </delete>

    <update id="updateEntity" parameterType="Auth">
        update <include refid="AuthTable"/>
        set <include refid="AuthUpdateMap"/>
        where auth_id = #{id}
    </update>

    <select id="getEntity" parameterType="Auth" resultMap="AuthMap">
        select <include refid="AuthColumns"/>
        from <include refid="AuthTable"/>
        <include refid="AuthWhereCondition"/>
    </select>
    
    <select id="getAuthsByUser" parameterType="User" resultMap="AuthMap">
        select <include refid="AuthColumns"/>
        from <include refid="AuthTable"/>
        join <include refid="RoleAuthTable"/> on auth_id = roleauth_aid
        join <include refid="RoleTable"/> on roleauth_rid = role_id
        join <include refid="UserRoleTable"/> on role_id = userrole_rid
        join <include refid="UserTable"/> on userrole_uid = user_id
        <include refid="com.techsen.tsweb.sys.dao.UserDao.UserWhereCondition"/>
    </select>
</mapper>
